{
  "name": "bike-spare-saas",
  "version": "1.0.0",
  "description": "Bike Spare Parts SaaS Platform",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm -C frontend run dev\" \"npm -C backend run dev\"",
    "build": "npm -C frontend run build",
    "start": "npm -C backend run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
